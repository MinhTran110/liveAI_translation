# Core dependencies
psutil>=5.9.0
rich>=13.0.0
python-dotenv>=1.0.0
requests>=2.28.0
numpy>=1.24.0
pytest>=7.0.0

# Asynchronous & Cloud ASR
websockets>=12.0
aiohttp>=3.9.0

# Audio capture (platform dependent)
sounddevice>=0.4.6
soundcard>=0.4.3

# Local Speech-to-Text (optional for cloud-only usage)
faster-whisper>=1.0.0

# Translation LLM clients
anthropic>=0.20.0
openai>=1.12.0
